# Server

## Methods

### `Server:GetPlayer(id): PlayerLobby`

Gets player at server tiwh given id

#### Parameters:
*   `id` (`number`) — Peer id of player

#### Returns:
*   `PlayerLobby` — Player object with this id