## Creating tower

We will give you some links what you need to read

First read [towers config](pages/towersconfig.md), and write your own config. So, tower will not work without `"Scene"` field, its spawns with tower spawn. Tp set up this scene read [setting up tower scene](pages/tower_scene.md).

So, also you need script, read about [tower class](pages/tower.md)